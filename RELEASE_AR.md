# إصدار الإصلاح والتطوير — 11 سبتمبر 2026

هذا الملف هو مرجع هذا الإصدار؛ الملاحظات القديمة المرفقة في المشروع تصف مراحل سابقة.

## التغييرات

- الدخول الناجح وحفظ كلمة المرور الجديدة ينتهيان عند `/profile.html`. حدث الاسترداد يفتح نموذج تغيير كلمة المرور، ولا يتجاوزه.
- معالج الاسترداد مركزي، وطلبات Supabase مؤجلة خارج قفل أحداث المصادقة. تجديد الرمز لا يمحو النموذج.
- بيانات جدول `profiles` تسبق بيانات التسجيل القديمة، مع حدود زمنية للأخطاء والطلبات وسياسات تمنع تعديل دور المستخدم من الواجهة.
- واجهة الدورات ترسل رمز Supabase؛ الخادم يتحقق منه عبر `/auth/v1/user` ويتطلب بريداً جامعياً مؤكداً. الهوية لا تؤخذ من جسم الطلب.
- قاعدة البيانات تسجل اجتياز كل درس في جدول منفصل. النسبة التي يرسلها المتصفح لا تكفي لإصدار الشهادات. الاختبارات تعليمية مفتوحة، وليست امتحاناً مراقباً؛ الإجابات موجودة ضمن محتوى الواجهة.
- ثماني دورات بمستويات مبتدئ ومتوسط ومتقدم: 24 حالة عملية، 72 درساً، نص عربي وإنجليزي، و8 مخططات SVG، مع خطوات تطبيق ومعايير نجاح.
- سجل الشهادات في الملف الشخصي، وإعادة الإصدار باللغة المختارة، وتنزيل SVG والطباعة إلى PDF من صفحة التحقق.
- تحسين اتجاه الصفحة والترجمة وحفظ اللغة دون مسح الجلسة أو الحقول غير المحفوظة. أزيل عنصر رفع الصورة غير العامل في النسخة الأصلية.
- حجب ملفات الخادم وSQL من الخدمة العامة، وإخراج السكربتات المضمنة إلى ملفات متوافقة مع CSP، والتحقق من TLS لقاعدة البيانات.

## التشغيل والنشر

1. استخدم Node.js 22.12 أو أحدث؛ بيئة الاختبار استخدمت 24.19.0. من جذر المشروع نفّذ:

   ```sh
   npm ci --prefix server
   npm test --prefix server
   ```

2. جهّز متغيرات بيئة Render:
   - `DATABASE_URL`: اتصال PostgreSQL الخاص بالمشروع، بحساب الخادم المالك للجداول، لا بمفتاح متصفح.
   - `SUPABASE_URL`: عنوان المشروع `https://...supabase.co`.
   - `SUPABASE_ANON_KEY`: المفتاح العام/القابل للنشر فقط. لا تستخدم `service_role` هنا؛ يقدم الخادم هذا المفتاح للمتصفح.
   - `FRONTEND_URL`: أصل الموقع النهائي عبر HTTPS، دون مسار إضافي.
   - `JWT_ACCESS_SECRET` و`JWT_REFRESH_SECRET`: قيم سرية عشوائية منفصلة للنظام القديم المحتفظ به في المشروع.
   - `NODE_ENV=production`، و`PORT` كما يضبطه Render.
   - لإرسال الشهادات بالبريد: `BREVO_API_KEY` و`EMAIL_FROM` الموثق، أو إعداد النقل البريدي المدعوم في المشروع. إصدار الشهادة لا يثبت تسليم البريد.

3. نفّذ `supabase/profile-setup.sql` في محرر SQL الخاص بالمشروع. شغّل ترحيلات الخادم بعد تعيين `DATABASE_URL`:

   ```sh
   npm run migrate --prefix server
   ```

   الترحيل `008_verified_lessons.sql` يضيف جدول نتائج مستقل مع RLS؛ لا يحذف تقدم الطلاب أو شهاداتهم القديمة. العرض الجديد يحسب التقدم من نتائج الدروس المتحققة. إعادة إصدار شهادة قديمة تستلزم إكمال هذه الاختبارات. بيانات التقدم القديمة تبقى مخزنة.

4. في Supabase Authentication URL Configuration أضف أصل الموقع إلى Site URL، وأضف عنوان `https://YOUR-HOST/reset-password.html` إلى Redirect URLs. فعّل تأكيد البريد. طبّق سياسة نطاق البريد في إعداد التسجيل أو Auth Hook أيضاً إذا أردت منع إنشاء الحسابات غير الجامعية ذاتها؛ الخادم الحالي يمنع استخدامها في التعلم، والواجهة تتحقق قبل التسجيل.

5. انشر باستخدام `render.yaml`، أو Build: `npm ci --prefix server` وStart: `node server/src/server.js`. التطبيق Node يخدم الواجهة وAPI معاً. لا تضف إعادة كتابة عامة تحول ملفات JS إلى HTML.

6. راجع على قاعدة الاختبار صلاحيات الجداول القديمة التي أنشأتها ترحيلات المشروع: يجب ألا تستطيع أدوار `anon` أو`authenticated` تعديل `student_course_certificates` أو جداول الهوية والتقدم عبر Data API. لم تُعدّل صلاحيات هذه الجداول جماعياً في هذا الإصدار، لأن أثرها في التطبيقات القائمة غير معروف. جدول النتائج الجديد مغلق عبر RLS. يلزم هذا الفحص قبل اعتماد إنتاجي.

7. إذا كان بريد موجوداً في نظام `users` القديم بمعرّف مختلف عن `auth.users.id` فستظهر استجابة 409. لا يتم دمج الحسابات تلقائياً بالبريد؛ يلزم ترحيل مدروس للعلاقات بعد التأكد من الملكية. لا تحذف الحساب القديم لتجاوز الخطأ.

8. إذا رفض الاتصال شهادة TLS، ثبّت سلسلة الثقة المعتمدة مثل `NODE_EXTRA_CA_CERTS` المناسبة لمزودك؛ لا تعطل التحقق من الشهادة.

## حدود الاعتماد

اجتازت النسخة اختبارات محلية ومحاكاة المصادقة. لم تتوفر جلسة إدارة Supabase أو Render أو قاعدة اختبار PostgreSQL متصلة؛ لذلك لم تُنفّذ الترحيلات على قاعدة حقيقية، ولم يُختبر إرسال بريد فعلي أو السباق المتزامن لإصدار الشهادات أو النشر العام. لا تمثل الحزمة اعتماداً إنتاجياً نهائياً قبل تلك الفحوص.

اختبر على بيئة staging: إنشاء حساب وتأكيده بالبريد الحقيقي، الدخول، رابط الاسترداد، تعديل الملف، مستخدمان مع محاولات الوصول المتبادل، اجتياز دورة كاملة، إعادة إصدار الشهادة باللغتين، تسليم البريد، ومراجعة صلاحيات Data API. اختبر أيضاً تجديد الرمز وعودة الاتصال والتزامن على قاعدة البيانات الحقيقية.

## الملفات والاختبارات

- المصدر الكامل في هذا المجلد. ملفات الواجهة الأصلية في الجذر، ونسخة متطابقة في `server/public` لدعم تخطيط Render البديل.
- `server/tests/release.test.js`: اختبار المحتوى، التقييم، SVG، قفل المصادقة، رفض الرموز، ومرونة تخزين اللغة.
- `server/tests/browser.test.cjs`: اختبارات متصفح قابلة لإعادة التشغيل؛ راجع رأس الملف. يستخدم حسابات اصطناعية وواجهات Supabase محاكية.
- لتشغيل اختبار المتصفح: ثبّت `playwright` محلياً كأداة اختبار، وشغّل الخادم على المنفذ 3000، ثم `node server/tests/browser.test.cjs`. يستخدم Chrome المثبّت أو المسار المحدد في `CHROME_PATH`؛ ويمكن تحديد مكان مكتبة Playwright في `PLAYWRIGHT_MODULE`. لا تثبّت أدوات الاختبار ضمن خدمة الإنتاج.
- `QA_REPORT_AR.md`: ملخص النتائج والقيود.
- نماذج الشهادات المرفقة تحمل بيانات اختبار، وليست شهادات صادرة لطلاب حقيقيين.

مراجع التحقق الفني: https://supabase.com/docs/guides/troubleshooting/why-is-my-supabase-api-call-not-returning-PGzXw0 وhttps://supabase.com/docs/reference/javascript/auth-onauthstatechange .
