# تشغيل التحقق بالبريد

## ما تغير
- إزالة الرمز ◈ وإطاره من واجهة التسجيل والدخول.
- إصلاح تحميل مكتبة Supabase وملفات إعدادها قبل المصادقة؛ كانت غير محملة في index.html.
- توحيد المصادقة على رابط البريد (Magic Link)، وإزالة حقول كلمات المرور التي كان الكود يتجاهلها أصلًا.
- التسجيل ينشئ الحساب ويحفظ بيانات النموذج في user_metadata؛ الدخول لا ينشئ حسابًا جديدًا.
- بعد قبول طلب الإرسال تظهر صفحة verify-email.html بعنوان «يرجى الانتظار للتحقق».
- التحقق الناجح يعيد المستخدم إلى index.html ويفتح واجهة النادي. تتزامن الجلسة بين تبويبات المتصفح نفسه؛ عند فتح الرابط على جهاز آخر يفتح النادي على ذلك الجهاز.
- إضافة إعادة إرسال مع مهلة، ورسائل فشل الاتصال والرابط المنتهي.
- تطبيق التعديلات في النسخة الأساسية وserver/public.

## إعدادات Supabase المطلوبة قبل التجربة الفعلية
1. ارفع الملفات المعدلة، بما فيها verify-email.html وملفات js وcss الجديدة، إلى استضافتك. لا تستخدم file://.
2. في Authentication > URL Configuration ضع عنوان موقعك الفعلي في Site URL. أضف العنوان الكامل لصفحة verify-email.html إلى Redirect URLs، مثل https://YOUR-DOMAIN/verify-email.html. إن كان الموقع تحت مجلد فأدرجه في المسار. المثال ليس عنوانًا فعليًا.
3. فعّل مزود Email والسماح بإنشاء حسابات جديدة. أبقِ Confirm Email مفعّلًا.
4. في قوالب Magic Link وConfirm signup اجعل زر التحقق يستخدم {{ .ConfirmationURL }}. لا تستبدله برابط الصفحة مباشرة ولا بقيمة {{ .Token }} وحدها.
5. اضبط Custom SMTP ببيانات مزود البريد وعنوان مرسل موثق. خدمة Supabase الافتراضية لا ترسل إلى عناوين خارج أعضاء فريق المشروع. لا تضع كلمة مرور SMTP داخل JavaScript.
6. راجع Auth Logs وسجلات مزود البريد عند فشل الإرسال أو ظهور 429. تحقق من مجلد الرسائل غير المرغوب فيها.

## اختبار بعد النشر
- أنشئ حسابًا ببريد جامعي تملكه ثم تأكد من ظهور شاشة الانتظار ووصول الرسالة.
- افتح أحدث رابط وتأكد من ظهور النادي، ثم سجّل الخروج وأعد تجربة الدخول.
- جرب إعادة الإرسال والرابط المنتهي وتبويبًا آخر في المتصفح نفسه.
- التسجيل يقبل نطاقي aou.edu.sa وaou.edu فقط، كما في المشروع الأصلي.

## حدود التحقق المنجز
نجح فحص صياغة JavaScript واختبار محلي بمحاكاة خدمة Supabase لحالات الانتظار، والجلسة المتحققة وغير المتحققة، والرابط المنتهي، وإعادة الإرسال، وغياب المكتبة، والدخول المباشر للصفحة، وترتيب التحميل وتطابق نسختي النشر. لم يتم اختبار بريد حقيقي أو تعديل لوحة Supabase أو نشر الموقع؛ هذه تحتاج حساب المشروع وعنوان الموقع. لا تثبت المحاكاة وصول البريد. وظائف الخادم الأخرى التي تستخدم نظام مصادقة منفصلًا لم تُغيّر.

## المراجع الرسمية
- https://supabase.com/docs/guides/auth/auth-email-passwordless
- https://supabase.com/docs/guides/auth/redirect-urls
- https://supabase.com/docs/guides/auth/auth-smtp
