# تقرير QA

البيئة: Windows، Node.js 24.19.0، Chrome headless، خدمة Express محلية. اختبار المتصفح يستخدم محاكاة Supabase وبيانات مستخدم اصطناعية؛ لا يتصل بحساب طالب حقيقي.

## اختبارات ناجحة

15 مجموعة سيناريوهات متصفح، دون أخطاء JavaScript مسجلة في المسارات المختبرة:

1. تحميل الاسم والتخصص والبريد من بيانات الملف، مع أولوية الجدول على metadata.
2. تبديل العربية والإنجليزية دون فقدان رمز الجلسة أو الاسم غير المحفوظ.
3. تجديد الرمز دون إعادة تحميل النموذج أو الدخول مجدداً في قفل المصادقة.
4. عرض الملف الشخصي على شاشة هاتف 390px دون تجاوز أفقي.
5. إعادة تعيين كلمة المرور والتوجيه إلى الملف الشخصي.
6. الدخول بكلمة المرور والتوجيه إلى الملف الشخصي.
7. حدث استعادة كلمة المرور وفتح نموذجها.
8. المحتوى الإنجليزي للدورة والرسم التوضيحي على الهاتف.
9. استجابات 404 لمسارات مصدر الخادم وSQL والملفات المفقودة.
10. خروج المستخدم من الملف يعيده إلى شاشة الدخول.
11. حماية الملف دون جلسة وإظهار خطأ الرابط غير الصالح لإعادة التعيين.
12. طلب ملف معلّق ينتهي برسالة خطأ بدلاً من استمرار التحميل.
13. التحقق من الشهادة وعرض تنزيل SVG والطباعة إلى PDF.
14. فشل التحقق لا يعيد إظهار الشهادة السابقة عند تبديل اللغة.
15. تبديل اتجاه ولغة صفحات الانضمام والاقتراحات والتأكيد والصيانة.

اجتازت اختبارات Node أيضاً: ثماني دورات و72 درساً بمحتوى ثنائي اللغة؛ صحة تصحيح الإجابات وحد 80% لكل درس؛ رفض دورة أو فهرس مجهول ومصفوفة إجابات غير صحيحة؛ ترميز SVG ضد حقن العناصر؛ رفض غياب الرمز والرمز غير الصالح والبريد غير المؤكد؛ وعدم تعطل محرك اللغة عند منع localStorage. اجتاز اختبار الشهادة الأصلي واختبار الانحدار بعد إزالة افتراضين قديمين غير متوافقين مع الواجهة الأصلية الحالية (اسم مفتاح واجهة وحظر خلفية زخرفية).

## ما لم يُختبر فعلياً

في استكمال هذا الإصدار أُنجزت ترجمة النصوص المتبقية في الصفحات الأساسية. اجتاز فحص النص الظاهر بالإنجليزية ثماني صفحات؛ لم يبق فيها نص عربي سوى زر اختيار العربية وبيانات اسم المستخدم. أُعيدت اختبارات المتصفح الخمسة عشر بنجاح بعد التعديل، وأُرفق ملف تشغيلها الفعلي في `server/tests/browser.test.cjs`. نتائج التشغيل واللقطات في `qa-artifacts`.

- تنفيذ SQL على PostgreSQL/Supabase، سياسات RLS القائمة وعزل حسابين حقيقيين.
- البريد الفعلي وتأخر رابط الاسترداد والإعدادات الفعلية لقائمة Redirect URLs.
- تزامن الكتابات وإصدار الشهادات على قاعدة بيانات حقيقية.
- نشر Render ومراجعة صحة TLS وشهادة مزود قاعدة البيانات.
- تدقيق ترجمة بشري شامل لكل نص قديم في الألعاب والأدوات والتجارب الفرعية. الاختبارات تثبت التبديل واتجاه الصفحات والمسارات الأساسية المذكورة، وليست شهادة ترجمة لكل نص في الموقع.

رفضت المراجعة الآلية اقتراحين واسعين ولم يُطبّقا: تصفير التقدم القديم بعد أرشفته، وسحب صلاحيات جماعي من كل الجداول المكتشفة. البديل المنفّذ هو جدول إضافي محمي لنتائج الدروس مع حفظ البيانات القديمة؛ صلاحيات الجداول القديمة تحتاج مراجعة محددة قبل الإنتاج.

النتيجة: اجتياز محلي للمسارات الأساسية مع المحاكاة؛ الاعتماد الإنتاجي النهائي مشروط بفحوص staging الواردة في RELEASE_AR.md.
