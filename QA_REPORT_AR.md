# تقرير التسليم والفحص — 14 سبتمبر 2026

## التعديلات الأخيرة
- تكبير أهداف اللمس لأزرار اللغة وتسجيل الدخول والاستعادة إلى 44px، وترتيب رأس صفحة الدورة على الجوال، وإظهار تركيز لوحة المفاتيح.
- إضافة ورش ببيانات اصطناعية وشرح تحليل ومعايير تحقق إلى مسارات التعلم؛ 8 مجموعات بيانات أساسية موزعة على مراحل التحليل والتطبيق والمراجعة في 72 درساً.
- الحفاظ على الرسومات التوضيحية SVG لكل مسار. توجد أغلفة ورسومات تدفق؛ لا توجد فيديوهات تعليمية جديدة أو صور فوتوغرافية للمختبرات.
- منع تبديل الدرس أثناء حفظ الإجابات وتثبيت إجابات الطلب قبل نافذة إصدار الشهادة.
- استعادة الشهادة الموجودة عند فتح الدورة، وإزالة وعد الإرسال البريدي التلقائي غير المضمون.
- إظهار خطأ واضح عند تعذر تحميل صورة الشهادة، ومنع الطباعة قبل تحميلها.
- معالجة حالة غياب خدمة المصادقة في الملف الشخصي كي لا تبقى عبارة التحميل ونموذج قابل للحفظ دون جلسة.

## اختبارات ناجحة في هذه المراجعة
- اختبارات regression وsmoke وrelease المرفقة في server/tests.
- 8 دورات، 72 درساً، 5 أسئلة لكل درس باللغتين، وجود خطوات التطبيق والنتائج المتوقعة والرسومات.
- احتساب اجتياز الاختبار على الخادم، ورفض المسار/الدرس/الإجابات غير الصالحة.
- رفض الطلبات غير الموثقة وحسابات البريد غير المؤكد.
- حفظ ملف موجود، إنشاء ملف مفقود، تنافس إنشاء الملف، ورفض تحديث غير مسموح.
- الحفاظ على اسم صاحب الشهادة ورقمها وتاريخها عند تغيير اللغة، وحماية النص من حقن HTML/SVG.
- استعادة كلمة المرور بالمحاكاة: لا يستهلك الرمز بمجرد فتح الصفحة، ولا تقبل جلسة أخرى بديلاً للتحقق، ويرفض الرمز الخاطئ.
- 14 اختبار HTTP للصفحات وأنواع ملفات CSS/JS/SVG وحماية ملفات الخادم والإعدادات.
- فحص صياغة JavaScript وتطابق نسخ الواجهة وغياب المراجع المحلية المفقودة: التفاصيل في final-audit.json.
- فحص تفاعلي للعرض باللغة الإنجليزية ومحتوى الورشة، وفحص أهداف اللمس على عرض390px.

## حدود يجب معرفتها
- المحتوى تمهيدي وتطبيقي بمستويات متفاوتة؛ ليس بديلاً عن منهج احترافي معتمد أو مختبرات سحابية مستضافة. الورش تشمل تطبيقات ورقية/محلية؛ لا ندعي وجود72 مختبراً مختلفاً.
- إصدار الشهادة مبني على اختبارات تعلم، وليس امتحان هوية مراقباً. يمكن للمتعلم مراجعة بيانات الأسئلة في المتصفح.
- تسليم بريد الاستعادة إلى الجامعة غير موثوق حتى الآن، وقد أُجّل باتفاق المستخدم. إعداد Gmail في Supabase قائم؛ كلمة مرور التطبيق غير مضمنة في الملفات.
- الطباعة مهيأة عبر نافذة المتصفح مع CSS للطباعة. لا يوجد تأكيد لطباعة ورقية فعلية في هذه المراجعة.
- لم تُنفذ دورة كاملة على بيانات طالب حقيقية ولم تُنشأ شهادات تجريبية في قاعدة الإنتاج. اختبارات الحالات الطرفية معزولة/محاكاة.
- الحزمة تحتوي آخر تغييرات محلية؛ لم ننشر هذه الجولة إلى GitHub، تلبية لطلب المستخدم رفع الملف بنفسه.
- لا ندعي اختبار كل متصفح وكل جهاز أو ضمان عدم وجود أي عطل مستقبلي.

## إعادة الاختبار
من مجلد server: npm ci ثم npm test. اختبارات المتصفح الاختيارية في server/tests/browser.test.cjs تتطلب Playwright ومتصفحاً محلياً وتشغيل الخادم؛ لم نعد تشغيل هذه المجموعة الآلية كاملة في هذه الجولة. الفحص التفاعلي تم عبر المتصفح المتاح.

مراجعة بصرية مستقلة: اجتازت الصفحة الرئيسية والدورة والتحقق من الشهادة مراجعة محدودة بعرض390 و1440px. عولج التفاف أعمدة الورشة بجعل التمرير داخل كتلة البيانات؛ لم تشمل المراجعة المستقلة ملفاً شخصياً موثقاً أو شهادة صالحة محملة من قاعدة محلية.

## تشديد أدوات الفحص
نجحت23 حالة اختبار إضافية لكاشف الروابط والملفات وكلمات المرور. حُصرت روابط التحليل في HTTP/HTTPS مع حد4096 محرف ورفض محارف التحكم، ورفع خطورة بيانات الدخول في الرابط والتنزيلات التنفيذية والعناوين المحلية. قراءة الملفات لا تتجاوز256KiB وبمهلة5 ثوان وحد25MiB، مع إعلان عدم اكتمال القراءة بدلاً من نتيجة آمنة. الأرشيفات لا تُفك ولا تُنفذ؛ محتواها الداخلي غير مفحوص. شُددت أنماط كلمات المرور المتكررة والقصيرة؛ لم تتغير سياسة تسجيل الدخول نفسها. أزيل ادعاء نسبة الأمان من العرض. هذه أدوات محلية استدلالية وليست خدمة مكافحة فيروسات أو فحص تسريبات/سمعة حي. مرجع القيود: https://cheatsheetseries.owasp.org/cheatsheets/File_Upload_Cheat_Sheet.html
