# النسخة المعدلة — نادي الأمن السيبراني

## التعديلات

- صفحتان مستقلتان للاقتراحات والاستفسارات والانضمام، بتصميم عربي متجاوب وألوان نيون سماوي وبنفسجي.
- جميع الحقول المطلوبة، مع رقم الجوال في النموذجين والتخصص وسبب الانضمام في طلب العضوية.
- روابط واضحة من شاشة الدخول وداخل الموقع، وتأثير نيون عند المرور والتركيز والضغط.
- إرسال مباشر إلى Supabase، مع مهلة انتظار 15 ثانية ومنع النقرات المتكررة ورسائل نجاح وفشل واضحة.
- إزالة تأخير الدخول الثابت 620 مللي ثانية، وإزالة مكتبة Supabase غير المستخدمة من الصفحة الرئيسية، وتأجيل تشغيل ملفاتها إلى ما بعد تحليل الصفحة، وإيقاف بعض الألعاب المتحركة عندما تكون خارج العرض أو المتصفح بالخلفية.
- مزامنة الملفات المعدلة مع server/public لتوافق طريقتي النشر الموجودتين.

## التشغيل والنشر

استخدم نفس طريقة تشغيل المشروع واستضافته الحالية. استبدل ملفات المشروع بهذه النسخة كاملة. صفحات النماذج هي suggestions.html وjoin.html. لتفعيل قاعدة البيانات اتبع SUPABASE-FORMS-SETUP.md وشغّل supabase/forms-setup.sql في مشروع Supabase الصحيح.

## التحقق

نجحت اختبارات محلية للحقول المطلوبة، حدود الطول، صيغة البريد، تطبيع رقم الجوال العربي، بيانات الطلبين، النجاح، رفض الخادم، انقطاع الشبكة، انتهاء المهلة، ومنع الإرسال المكرر. الاختبارات تستخدم استجابات محاكاة ولا ترسل رسائل حقيقية. نجحت فحوص صياغة JavaScript وتطابق نسختي الملفات.

لم يتم نشر الموقع على استضافة عامة أو تنفيذ إعدادات قاعدة البيانات في حساب Supabase. التحسينات تقلل عمل المتصفح، ولا تمثل ضمانًا لزمن تحميل معين على كل الأجهزة والشبكات أو لمعالجة بطء خادم الاستضافة.
