const buttons=document.querySelectorAll('[data-view]'),forms=document.querySelectorAll('.form'),notice=document.getElementById('notice');function show(view){forms.forEach(f=>f.classList.toggle('active',f.id===view));document.querySelectorAll('.switch button').forEach(b=>b.classList.toggle('active',b.dataset.view===view));notice.textContent=''}buttons.forEach(b=>b.addEventListener('click',()=>show(b.dataset.view)));document.querySelectorAll('form').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault();notice.textContent='هذه معاينة فقط — لن يتم إرسال البيانات.'}))